// usuario que se muestra en la lista (no lleva los campos de support)
export interface SmallUser {
    id: number;
    email: string;
    first_name: string;
    last_name: string;
    avatar: string;
}
